# jhonstart/modules/jhonstart/src/

> Path: `repository/jhonstart/modules/jhonstart/src/`
> Parent (workspace): [`../../../AGENTS.md`](../../../AGENTS.md)

Source for the `jhonstart` **core member** — the workspace member
`modules/jhonstart/`, which is what `from "jhonstart"` resolves to (the umbrella
`repository/jhonstart/botopink.json` is a workspace and answers no import).
Its manifest is `../botopink.json`: `src: "src/"`, `entry: "root.bp"`,
`target: "commonJS"`, and the `files` list below as the consumer surface; it
declares no `targets` and so inherits the workspace's `["commonJS", "erlang"]`.
The UI **core and hooks are real botopink** — an `Element` tree, builders, a
synchronous SSR renderer and the hook family, all implemented in `.bp` (no host
intrinsics, no async). The `html """…"""` comptime expander left this member
for `modules/jhonstart-html/` (front 95; see `../../jhonstart-html/src/AGENTS.md`). Only the genuinely host-bound surface
(the Http context) stays as a `.d.bp` **declaration**, carrying an explicit
"STILL GATED" note naming the gap; `router.d.bp` was promoted to `router.bp` by
front 26. `botopink test` compiles
the `.bp` files and runs their `test {}` blocks; the `.d.bp` files are type
surface for consumers.

| File | Kind | Provides |
|---|---|---|
| `element.bp` | **compiled** | `type Element(…) implement @Context<Element>` with a `Children` `children` field (the UI node AND the hook ContextBase), builders (`text`, `fragment`, `div`/`span`/`p`/`h1`/`ul`/`li` — take `Children`), `renderToString` (pure, synchronous), `test {}` (render + the G4 coercion) |
| `hooks.bp` | **compiled** | `type State<T>(value, set: fn(next: T))` (G1) + `state`/`effect`/`memo`/`ref`/`reducer` — nouns, no `use` prefix — returning `@Component<ElementBase, _>` with real server-pass bodies (G2 labeled tuple types `#(current: T)`/`#(state: S, dispatch: fn(action: A))`), `test {}` calling the bodies directly + a custom hook `counter` (`-> @Component<ElementBase, State<i32>>`) and a component `Counter` (`-> @Component<ElementBase, Element>`, `use state`/`use memo`), rendered plainly as the server pass |
| `elements.bp` | **compiled** | the **element surface** (front 94): `el(tag, children, attrs)` and `voidEl(tag, attrs)` — the only two places an `Element` is built here — the predicates `isVoidTag`/`isRawTextTag` a void-aware renderer consults, and the HTML tags `element.bp` does not declare, each in `element.bp`'s exact shape (`fn <tag>(children: Children, attrs: Array<#(string, string)> = []) -> Element`). `el` is the escape hatch for an unnamed tag. Attribute values are stored verbatim — escaping belongs to the renderer |
| `client_runtime.bp` | **compiled** (`mod`, not `pub mod`) | `clientRender(component, commit)` — the one `#[@External.Node("./client_runtime.mjs", "render")]` cell; its require ships the sidecar (G2). Never called from erlang. The five nouns are deliberately not re-declared (a second `state` shadows `hooks.state` across the flat package surface) |
| `client_runtime.mjs` | host (js) | the client build's hooks: `state`/`effect`/`memo`/`ref`/`reducer` with hook semantics + `render(component, commit)` over jhonstart's own render cycle (cursor-indexed cells, microtask-batched re-render, effects after commit by `deps`); first-render values outside a render. The client build resolves `jhonstart/hooks` to it (bundler, front 68); `../../../examples/jhonstart-counter/client.mjs` does it under node. Shipped into a consumer's `out/jhonstart/` by `shipMjsSidecars` — see the workspace `AGENTS.md` § Known defect for when that silently misses |
| `router.bp` | **compiled** | the **route snapshot** (front 26): `type RouterState(path, params, search, pattern, selected)` with `param`/`searchParam`/`segments`/`segment`, the package's ONE pair-list decoder `pairValue` (first match wins), `patternSegments`/`segmentAt`; the pair codec is std's `encoding.formParse`/`formStringify` (decision 116 rule 4), no copy here; `snapshot()` over five dual-target cells and the one writer `fill(path, params, search, pattern, selected)`; the six hooks (`router`/`pathname`/`params`/`searchParams`/`selectedLayoutSegment`/`selectedLayoutSegments`, each `-> @Component<ElementBase, _>`) and the six navigation verbs (`push`/`replace`/`back`/`forward`/`refresh`/`prefetch`) with `lastNavigation()` to read what one recorded (`refresh()` carries `actions`' `refreshValue()`); `navigationFor`/`applySignal` read an envelope's `n` with `routing`'s `signalFromWire`, and `resolveRoute(table, path, search)` matches with `routing`'s `parseTable`/`matchPath` — no matcher, no table parser and no signal grammar of its own. `Link` did NOT come along: it is front 27's `link.bp`, in the `jhonstart-link` member |
| `router_runtime.mjs` | host (js) | the router's store (a module-global) and the History API half of `navigate`. The js twin of `sidecars/jhonstart_router.erl`, cell for cell, so `test/router_test.bp` is one set of assertions run on both rows |
| `sidecars/jhonstart_router.erl` | host (BEAM) | the same cells over the CALLING PROCESS's dictionary — a request is a process, the snapshot dies with it, two concurrent renders cannot see each other's route. **Not a `files` entry**: `shipErlSidecars` reads the `atom:fun(` qualifiers out of the emitted erlang and copies `src/sidecars/<atom>.erl`. The atom may not be `router` — an atom matching a module the build emitted is skipped, silently |
| `server.bp` | **compiled** | the **request** (front 28, promoted from `server.d.bp`): `type RequestData(method, path, params, query, headers, cookies)` with `param`/`queryParam`/`header`/`cookie` — each a plain `string`, `""` when absent, all four through front 26's `pairValue`; `request() -> @Component<ElementBase, RequestData>` (a hook: the return is the effect) over six dual-target cells, the writer pair `enterRequest(req)`/`leaveRequest()` (front 30's render calls both; a read outside them RAISES), the two shortcuts `cookies()`/`headers()`, and `renderServerComponent(component: fn() -> @Task<Element>) -> @Task<string>` (and `renderComponent` for a `fn() -> @Component<ElementBase, Element>` thunk) — one `await`, then a synchronous render, over an **unstarted thunk**. The pair-shaped values are stored with std's `encoding.formStringify` and decoded with `encoding.formParse`. The `Http` phantom base and the `Request` behavior did NOT come along (every hook anchors at `ElementBase`, botopink decisions 96/128). Ships no escaping (std's `escape`, called by front 30's walker) and no parallel awaiting (front 02's) |
| `server_runtime.mjs` | host (js) | the request store (a module-global). The js twin of `sidecars/jhonstart_server.erl`, cell for cell, so `test/server_test.bp` is one set of assertions run on both rows. It exists on this row because an **erlang-only cell reds the commonJS COMPILE at its call site** — measured, workspace `AGENTS.md` § *Why the request cells are jhonstart's own* |
| `client.bp` | **compiled** (front 29), PURE | the **server/client boundary**: `#[client]` (emits a PURE `__jhClient_<Name>() -> string`; refuses a non-`fn` and a fn whose reflected `returnType` is not `Element`, so a data-loading `-> @Task<Element>` server component — reflected `"Task"` — cannot be marked client) and `#[clientProps]` (on the props RECORD, because `@Decl` exposes no function parameters; whitelist `string`/`i32`/`f64`/`bool`, four names and not the spec's six because `Field.typeName` erases an array's element type), `islandId`/`islandAttrOf`/`islandAttr` — decision 77, `islandAttrOf` being the ONE occurrence of `"data-jh-i"` in this tree — `Island` + `clientMount` + `islandEntry` (std's `encoding.formStringify`) + `propsOf`, `serverSlotAttr`/`serverSlot` (`data-jh-s="1"`, the Context-Provider hole) and the `serverOnly` poison pill. NO host cell of any target: `hydrate()` and `propsFor(name)` name front 68's generated `jhonstart/client-runtime` and are absent rather than stubbed. **It enforces four comptime refusals and nothing about the module graph** — front 68 is what makes the boundary a guarantee |
| `suspense.bp` | **compiled** (front 30) | `Boundary(id, fallback, child)` — the child an UNSTARTED `fn() -> @Component<ElementBase, Element>` thunk, because `@Task` is eager on erlang — `Suspense(b)` (the `data-jh-h` hole with the fallback; it registers `b` with the render through `render.mjs` / `jhonstart_render`, since an `Element` has nowhere to carry the thunk) and `holeId(n)` (`h<n>`) |
| `render.bp` | **compiled** (front 30) | `renderNode` — the escaping walker: `#text` through std's `escape.html`, attributes through `escape.attribute`, void tags unclosed (front 94's `isVoidTag`), `script`/`style` verbatim with `</script` / `</style` REFUSED (front 94's `isRawTextTag`), `#raw` verbatim — `raw`, `shellHtml`; `mountIsland` (the render's island ordinals, front 29's `islandEntry`); `compose(chain, route, page)` (layouts run first over a `#children` placeholder, `data-jh-t` per render, error / loading / `data-jh-n` levels); `Payload`/`writePayload` (std `json` + `escape.scriptJson`, no JSON writer of its own), `payloadScript`, `documentStart`/`documentEnd`; `RenderHooks(headExtra, bodyExtra)` + `setHooks`; `resetRender`/`renderedIslands`/`registeredBoundaries`/`componentOutcome`/`attemptTask`/`attemptRender` — the per-render cells reached from outside this directory go through these, never the cells (on commonJS a cell called from another directory requires its own copy of `render.mjs`) |
| `streaming.bp` | **compiled** (front 30) | `Chunk`/`resolve`/`fillHtml`, `redirectSignalHtml`/`notFoundSignalHtml`, `Response(status, header, write, close)` + `guarded`, `PageInput`, `App(plugins, allowedRedirects)` with `render`/`renderStream` (→ `@Task<@Result<void, string>>`), `app`, `redirectAllowed`, `extrasProblem`. Streaming is `eachCompleted` over one thunk per boundary (a process each on erlang, values handed back to the render's process in COMPLETION order). A signal before the first chunk is 404 / 307 through the `Response`, after it `data-jh-g` markup; a redirect target is checked against `PageInput.table` (`routing`'s `matchPath`) or `allowedRedirects`. `App` is declared LAST in the file: on erlang a method only reaches a function declared before its type |
| `plugin.bp` | **compiled** (front 30) | `type Json = string`; `RenderPlugin(name, head, chunk, close, payload)` — a record of async functions (an array of two different `behavior` implementors does not type) |
| `globals.bp` | **compiled** (front 30) | the registry `["payload", "fill", "signal"]`, `alias(name)` → `__bp<index>`, `globals() -> Globals` (a function: a `pub val` of a record type does not cross modules), `readPayload` (std `json.decode` over the payload text), `registerFill`, `registerSignal` — the browser cells, erlang twins answering empty |
| `routes.bp` | **compiled** (front 30) | `PageContext`, `LayoutProps(route, children, slots)`, the registry cells (`jhRegister*`) and `jhPage`/`jhLayout`/`jhTemplate`/`jhDefault`/`jhLoading`/`jhError`/`jhNotFound`, `uiTable()`, `ctxParam`/`ctxRest`; the markers `#[page]` (+ the `<name>Params` accessor), `#[layout]`, `#[template]` (all three refuse anything but `-> @Component<ElementBase, Element>`) and `#[defaultView]`; `Segment` + `segment`/`withLayout`/`withTemplate`/`withError`/`withLoading`/`withNotFound`/`segmentFor`/`pageFor`. The application site imports what a marker emits |
| `render.mjs` · `routes.mjs` · `sidecars/jhonstart_render.erl` · `sidecars/jhonstart_routes.erl` | host | the per-render state (process dictionary on the BEAM; module globals on node), `eachCompleted`, the browser fill/signal functions and payload reader; the UI registry (ETS + owner process on the BEAM). Sidecars avoid `.slice()` — the commonJS prelude patches it |
| `error_boundary.bp` | **compiled** (front 31) | the **error boundary**: `ErrorInfo(message, digest)`, `ErrorBoundary(id, fallback, child)` whose child is a `fn() -> @Result<Element, string>` thunk called EXACTLY ONCE through `__jhCapture`; `renderBoundary` (re-raises a signal) / `renderBoundaryChecked` (`Error(reason)` for a signal) / `catchError`; `infoFor` (no message) / `serverInfoFor` (message) over std's `hash.contentHash`; `wrap` (`data-jh-e`), `resetAttr` (`data-jh-reset`); `notFound()` / `redirect(url)` RAISE `routing`'s `nav:` reasons (a `@Component` body cannot `throw`), `isSignal` is `routing`'s `isSignalReason`. Host halves `signal_runtime.mjs` / `sidecars/jhonstart_signal.erl` (`raise`, `capture`, `captureTask`) |
| `metadata.bp` | **compiled** (front 32), PURE | the **head**: `Metadata(title, titleTemplate, description, openGraph, twitter, icons)` with `OpenGraph`/`TwitterCard`/`Icons`, and the separate `Viewport`; `empty*` constructors (no optionals — `""` and `[]`), `pick`/`pickList`/`applyTemplate`, `mergeMetadata` (strings replace when non-empty, lists replace wholesale, records field by field, the parent's template applied once to the child's title) and `mergeViewport`; `renderHead` / `renderViewport` write one tag per line in a fixed order through std's `escape.html` / `escape.attribute`. File routes (`sitemap`, `robots`, `manifest`, `opengraph-image`) are rakun front 66's |
| `sidecars/jhonstart_server.erl` | host (BEAM) | the request's six cells + `fill/6` over the CALLING PROCESS's dictionary — a request is a process, the request data dies with it, two concurrent renders cannot see each other's cookies. **Not a `files` entry**, same discovery as the router's. The atom is not `rakun_request_context`: rakun's member is `targets: ["commonJS"]` and has no BEAM row to bind to |

The four language gaps the framework surfaced are closed (spec
`jhonstart-language-gaps`, v0.beta.6) and now **used** here: types carry
function-typed fields (`set: fn(next: T)`, G1 — `State<T>` in `hooks.bp`),
labeled tuple types annotate transient hook shapes (G2 — `#(current: T)`,
`#(state: S, dispatch: fn(action: A))`), a function type returns an array (G3), and
`Element[]`/`Element`/`string` coerce into a `Children` parameter (G4 — the
builders' `children` arg and the `Element.children` field). The list form
(`div([a, b])`) is the rendering contract; the single/`string` forms type-check
(asserted in `element.bp`) but their render is the recorded normalization
follow-up (no runtime type tags to branch a lone child vs a list on).

This port also relies on a generic **cross-module** fix (a local component
returning an imported `Element`, fed to an imported `renderToString`/`use`, now
type-checks) — see `modules/compiler-core/src/comptime/AGENTS.md`
(`type_decl_registry` + `resolveTypeName`). It is lib-agnostic; the
`grep -riE "rakun|jhonstart"` gate stays clean.

Update this index in the same change that adds/renames a file, and keep
`../botopink.json`'s `files` list in sync — `root.bp` first, then every module a
consumer may import. A workspace member that is a library and lists no `files`
ships nothing: `botopink test` inside it fails and every runner cell reads
`✗ ships nothing`.

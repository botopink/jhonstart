// A page is a component (decision 117): `-> Element` is refused, located at
// the annotation.
import {page, jhPage, PageContext, ctxParam, ctxRest, Element, div, text} from "jhonstart";

#[page("blog")]
pub fn blog(route: PageContext) -> Element {
    return div([text(route.pathname)], attrs: []);
}

// A layout is a component (decision 117): `-> Element` is refused, located at
// the annotation. This is the guide's `OldLayout`.
import {layout, jhLayout, LayoutProps, Element, div} from "jhonstart";

#[layout("")]
pub fn OldLayout(props: LayoutProps) -> Element {
    return div([props.children], attrs: []);
}

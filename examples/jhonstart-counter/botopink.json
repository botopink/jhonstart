{
  "name": "jhonstart-counter",
  "version": "0.0.1",
  "description": "jhonstart showcase — a `use state` counter rendered server-side and then re-rendered under the client runtime by `client.mjs`; mirrors AGENTS.md § The client runtime",
  "src": "src/",
  "entry": "main.bp",
  "target": "commonJS",
  "targets": ["commonJS"],
  "dependencies": {
    "jhonstart": {
      "workspace": true
    }
  }
}
